<%= form_with(model: choice) do |form| %>
  <% if choice.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(choice.errors.count, "error") %> prohibited this choice from being saved:</h2>

      <ul>
        <% choice.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :choice_no, style: "display: block" %>
    <%= form.number_field :choice_no %>
  </div>

  <div>
    <%= form.label :choice, style: "display: block" %>
    <%= form.text_area :choice %>
  </div>

  <div>
    <%= form.label :answer, style: "display: block" %>
    <%= form.check_box :answer %>
  </div>

  <div>
    <%= form.label :question_id, style: "display: block" %>
    <%= form.number_field :question_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
